% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{estimateSNR}
\alias{estimateSNR}
\title{Estimate a time series Signal to Noise Ratio (SNR) given
total and noise variance components and (optionally) a background noise model.}
\usage{
estimateSNR(totalVar, noiseRaw, eps, betas, features, noise_mode = "floor")
}
\arguments{
\item{totalVar}{Total time series variance. Typically calculated with \code{estimateNoise}}

\item{noiseRaw}{noise variance estimate. Typically calculated with \code{estimateNoise}}

\item{eps}{Numeric. Lower bound applied to both noise and signal.}

\item{betas, features}{Coefficients (\eqn{betas}) and predictors (\eqn{features})
for the background noise model. Ignored when \code{noise_mode = "raw"}.
Both vectors must have the same length when used. The predictors form a
\emph{design vector}: include a constant \code{1} if the model has an intercept.
For example, for \eqn{baseline = b0 + b1 * mean}, use
\code{betas = c(b0, b1)}, \code{features = c(1, mean)}.}

\item{noise_mode}{Character scalar: one of \code{"raw"}, \code{"model"}, \code{"floor"}.
- \code{"raw"}   → use \code{noise_raw}
- \code{"model"} → use \code{baseline = sum(betas * features)}
- \code{"floor"} → use \code{max(noise_raw, baseline)}}
}
\value{
Named numeric vector with elements:
- snr (log2 scale)
- signal
- noise final estimate used for signal and snr calculation
- noise baseline (sum(betas * features); NA if not applied)
- noise raw
}
\description{
Given time series variance components (total variance, raw noise variance),
compute the final noise, signal, and SNR. For noise
estimation use one of:
\enumerate{
\item \strong{A raw noise variance} e.g the result of \code{estimateNoise}
\item \strong{A background noise model} based on linear predictors
\item \strong{A flooring rule}: final noise = max(raw, background)
}
The background noise model is a \emph{general} linear noise baseline:
\code{baseline = sum_i beta[i] * feat[i]}.
}
\examples{
estimateSNR(totalVar = 1.0, noiseRaw = 1.0, eps = 0.01,
            betas = c(0.5, 2.0), features = c(2.0, 4.5),
            noise_mode = "model")
}
