% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addAssay.R
\name{addReadLevelAssay}
\alias{addReadLevelAssay}
\title{Add a read-level assay to a SummarizedExperiment object}
\usage{
addReadLevelAssay(
  se,
  assayDF,
  assayName,
  replaceExisting = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{se}{A \code{\link[SummarizedExperiment]{SummarizedExperiment}} object.}

\item{assayDF}{A \code{\link[S4Vectors]{DataFrame}} with the same
dimensionality as \code{se}. Each column should be a read-level
\code{\link[SparseArray]{NaMatrix}} for a single sample.}

\item{assayName}{A character scalar giving the name of the new assay.}

\item{replaceExisting}{A logical scalar indicating whether to replace an
existing assay with the same name or not.}

\item{verbose}{Logical scalar. If \code{TRUE}, report on progress.}
}
\value{
A \code{\link[SummarizedExperiment]{SummarizedExperiment}} object with the
new read-level assay added.
}
\description{
Add a read-level assay to a SummarizedExperiment object
}
\examples{
library(SummarizedExperiment)
library(S4Vectors)
modbamfile <- system.file("extdata", "6mA_1_10reads.bam",
                          package = "SingleMoleculeGenomicsIO")
se <- readModBam(bamfiles = modbamfile, regions = "chr1:6940000-6955000",
                 modbase = "a", verbose = TRUE,
                 BPPARAM = BiocParallel::SerialParam())
# duplicate the 'mod_prob' assay into a new assay named 'new_assay'
se <- addReadLevelAssay(se, assayDF = assay(se, "mod_prob"),
                        assayName = "new_assay")
assayNames(se)
metadata(se)$readLevelData

}
\author{
Charlotte Soneson
}
