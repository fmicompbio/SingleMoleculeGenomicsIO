% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checkValidity.R
\name{checkSEValidity}
\alias{checkSEValidity}
\title{Check internal consistency of SummarizedExperiment object}
\usage{
checkSEValidity(se, verbose = FALSE)
}
\arguments{
\item{se}{A \code{SummarizedExperiment object}.}

\item{verbose}{A logical scalar. If \code{TRUE}, report on progress.}
}
\value{
Silently returns \code{NULL}. If the object is not valid, an error
will be raised.
}
\description{
All assays with read-level data must have the same number and order of
the reads, which must also agree with the order in \code{se$QC} if that
exists. All assays must have the same column names, which must also
agree with the column names of the object, and the \code{sample} column
in the \code{colData}.
}
\examples{
library(GenomicRanges)
modbamfiles <- system.file("extdata", c("6mA_1_10reads.bam",
                                        "6mA_2_10reads.bam"),
                           package = "SingleMoleculeGenomicsIO")
se <- readModBam(bamfiles = modbamfiles, regions = "chr1:6940000-6955000",
                 modbase = "a", verbose = FALSE,
                 variantPositions = GPos(seqnames = "chr1",
                                         pos = c(6940000, 6940500)),
                 BPPARAM = BiocParallel::SerialParam())
checkSEValidity(se)

}
\author{
Charlotte Soneson
}
