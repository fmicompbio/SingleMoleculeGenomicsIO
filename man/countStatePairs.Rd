% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/countStatePairs.R
\name{countStatePairs}
\alias{countStatePairs}
\title{Count pairs of modified bases by distance and modification state}
\usage{
countStatePairs(
  bamfile,
  regions = ".",
  modbase,
  nAlnsToSample = 0,
  seqnamesToSampleFrom = character(0),
  threshUnmod = 0.5,
  threshMod = 0.5,
  windowSize = 200,
  minMapQ = 0,
  minAlignedLength = 0,
  BPPARAM = MulticoreParam(4L),
  verbose = FALSE
)
}
\arguments{
\item{bamfile}{Character scalar with the path to a \code{modBAM}
file, containing information about base modifications in \code{MM} and
\code{ML} tags. The \code{bamfile} must have an index.}

\item{regions}{A \code{\link[GenomicRanges]{GRanges}} object specifying which
genomic regions to extract the reads from. Alternatively, regions can be
specified as a character vector (e.g. "chr1:1200-1300", "chr2:-6000",
"chr1:10-", "chrM" or ".").}

\item{modbase}{Character scalar defining the modified base.}

\item{nAlnsToSample}{A numeric scalar. If non-zero, \code{regions} is ignored
and approximately \code{nAlnsToSample} randomly selected alignments on
\code{seqnamesToSampleFrom} are read from the \code{bamfile}.
In order to make the results reproducible, make sure to set the
random number seed using \code{set.seed}. Please note that secondary and
supplementary alignments in \code{bamfiles} contribute to the total
number of alignments but will not be sampled, thus the number of used
alignments may be lower than \code{nAlnsToSample}.}

\item{seqnamesToSampleFrom}{A character vector with one or several sequence
names (chromosomes) from which to sample alignments from (only used if
\code{nAlnsToSample} is greater than zero).}

\item{threshUnmod, threshMod}{Numeric scalars defining how to convert
modification probabilities \code{p} to modification states. Bases with
\code{p < threshUnmod} will be considered unmodified, and bases with
\code{p >= threshMod} modified. All remaining bases will be ignored.}

\item{windowSize}{Numeric scalar giving the maximum window size
covering pairs of modified bases to consider in pair-counting mode.
A window size of 1 corresponds to a single base, a size of 2 to
directly adjacent bases, etc.}

\item{minMapQ}{Numeric scalar giving the minimal mapping quality to include
alignments in pair-counting mode.}

\item{minAlignedLength}{Numeric scalar giving the minimal alignment length
to include alignments in pair-counting mode.}

\item{BPPARAM}{A \code{\link[BiocParallel]{BiocParallelParam}} object that
controls the number of parallel CPU threads to use for some of the steps
in \code{readModBam()}. The default value is
(\code{\link[BiocParallel]{MulticoreParam}(4L, RNGseed = 42L)}).
If randomly sampling reads (\code{nAlnsToSample > 0}), make sure to set
the \code{RNGseed} argument when constructing the \code{BPPARAM} object
for reproducible results (see also
\code{vignette("Random_Numbers", package = "BiocParallel")}).}

\item{verbose}{A logical scalar. If \code{TRUE}, report on progress.}
}
\value{
A \code{DataFrame} with \code{windowSize} rows and five columns.
The \code{metadata} slot contains header information from \code{bamfile}.
}
\description{
For all pairs of bases with modification calls in a read, tabulate the
number of pairs at a given distance with a given modification state.
}
\examples{
modbamfile <- system.file("extdata", "6mA_1_10reads.bam",
                          package = "SingleMoleculeGenomicsIO")
res <- countStatePairs(bamfile = modbamfile,
                       regions = "chr1",
                       modbase = "a", windowSize = 300,
                       BPPARAM = BiocParallel::SerialParam())
res

}
\author{
Charlotte Soneson, Michael Stadler
}
